import React from "react";

export default function Counter() {
  return (
    // <button onClick={() => setCount((count) => count + 1)}>
    //     count is {count}
    //   </button>
    <div>Button</div>
  );
}
